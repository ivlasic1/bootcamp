<config-template xmlns="http://tail-f.com/ns/config/1.0"
                 servicepoint="loopback">
  <devices xmlns="http://tail-f.com/ns/ncs">
    <!-- DEVICE -->      
    <device>
      <name>{/device}</name>
      <config>
        <!-- IOS -->
	<?foreach {interface}?>
	<interface xmlns="urn:ios">
	  <Loopback>
	    <name>{loopback-intf/intf}</name>
	    <ip>
	      <address>
	        <primary>
	          <address>{loopback-intf/ip-address}</address>
		  <mask>255.255.255.255</mask>
	        </primary>
	      </address>
	    </ip>
	  </Loopback>
	</interface>
	<?end?>
	<!-- IOS-XR -->
	<?foreach {interface}?>
	<interface xmlns="http://tail-f.com/ned/cisco-ios-xr">
	  <Loopback>
	    <id>{loopback-intf/intf}</id>
	    <ipv4>
	      <address>
		<ip>{loopback-intf/ip-address}</ip>
		<mask>255.255.255.255</mask>
	      </address>
	    </ipv4>
	  </Loopback>
	</interface>
	<?end?>
      </config>
    </device>
  </devices>
</config-template>
